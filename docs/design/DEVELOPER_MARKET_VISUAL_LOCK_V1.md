# Developer Market Visual Lock v1

**Law:** Start with the business outcome, not the API vocabulary.

The builder tells SecurePay what was built and what money should do. SecurePay maps the topology underneath and presents only supported connection paths for the current environment.

The Living SecurePay Mark acts as a guide. It does not certify connection truth merely because code has been generated.

Sandbox and production are visually and semantically separate. Permanent secrets never belong in an AI conversation.
